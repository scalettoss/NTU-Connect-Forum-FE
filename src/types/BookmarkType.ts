export interface BookmarkResponseType {
    postId: number,
    fullName: string,
    postTitle: string,
    postContent: string,
    categoryName: string,
    createdAt: Date,
    postSlug: string,
    categorySlug: string,
}
